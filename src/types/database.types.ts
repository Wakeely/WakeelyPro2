// PLACEHOLDER — replace this entire file by running:
//   npx supabase login
//   npx supabase link --project-ref <your-project-ref>
//   npm run supabase:types
//
// That command inspects your live Supabase schema and generates exact,
// always-in-sync TypeScript types for every table/column/enum. Until you've
// run it, this stub keeps the app compiling with loose types.

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
